var genOutput = function(body) {
  // get global app variables
  var DEBUG = app.get('DEBUG');
  var svrAddr = app.get('svrAddr');
  var dataDir = app.get('dataDir');
  var mediaDir = app.get('mediaDir');

};
